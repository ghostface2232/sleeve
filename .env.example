# Cloudflare Worker proxy base URL for web builds.
# Convention: ${EXPO_PUBLIC_API_PROXY}/?url=<encoded upstream URL>
# Leave empty to call Odesli / iTunes directly (will hit CORS on web).
EXPO_PUBLIC_API_PROXY=
